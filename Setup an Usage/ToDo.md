1. Put all services into a bundle, test and document them
2. Check networking is OK when starting the lab (use fixed IPs?)
3. Deploy redis dor the gateway enrollment with Geoff Portal
4. Remove the certs that are outdated?
5. Replicate the networking config for the main gateway (so that it does not display Kubernetes fake cert)
6. Architecture diagram

Demo
* OTK use cases
    DCR
    CBAT
    Audience Restriction
    Token exchange

* Distributed rate limit
* HTTP2
* Otel baggage / custom metrics
* Circuit breaker
* Schema validation (XML/Json/Graphql)
* PQC encryption
* Removal of a JSON field: JSON-XML / XPATH /  XML->JSON
* Validation (xml, json, graphQL)

* Remove the git on the VM platform
* Re-create the test Token policy (without OTK dependencies)
* Put the data in the VLP directory
* Explain the directories in the doc